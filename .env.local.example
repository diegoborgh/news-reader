# Copy this file to `.env.local` and fill in the value.
# `.env.local` is gitignored and never committed.

# Currents API key — https://currentsapi.services/
# Free tier: 1,000 requests/day. Used only in server-side code (never exposed to the client).
CURRENTS_API_KEY=
